# SDK Modules

Language SDKs live in separate subdirectories. Keep each SDK self-contained so it can later be published, tested, and versioned independently.

Initial layout:

- `python/` - Python SDK for controlling the APK through ADB.
- `java/` - Java SDK for controlling the APK through ADB.
